import React from 'react'
import '@styles/Header.sass'
import '@styles/Slider.sass'

const Header = () => {
	return <header className="slider header-slider">FramezStudio</header>
}

export default Header
