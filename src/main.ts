import { SMatrix } from './entities/classes/sMatrix'
import './style.css'
import { eucDistance } from './utils/algebra'
let e, a, l, i
e = 120
a = 200
l = 500
i = 400

let m = new SMatrix(e, l, a, i)
// m.print()
