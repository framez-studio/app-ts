import { describe, it } from 'vitest'

describe('Elements utils', () => {
	it('should return a matrix with size according to the number of degs of freedom', () => {})
	it.todo(
		`should generate a matrix from a list of IElement's and INode's`,
		() => {},
	)
})
